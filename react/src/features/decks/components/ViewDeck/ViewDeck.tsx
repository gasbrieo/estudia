import type { FC } from "react";

import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const ViewDeck: FC = () => {
  return (
    <Box>
      <Typography variant="h6">ViewDeck</Typography>
    </Box>
  );
};

export default ViewDeck;
